# Clash Verge 配置文件

# 代理设置部分
proxies:
  # 新的 SOCKS5 代理
  - name: "New-Proxy"
    type: socks5
    server: 66.253.2.14
    port: 11501
    username: mgz
    password: yob

  # 直连配置
  - name: "Direct"
    type: direct

# 代理规则部分
rules:
  # 访问以 .claude.ai 结尾的域名时，使用 SOCKS5 代理
  - DOMAIN-SUFFIX,claude.ai,New-Proxy

  # 访问包含 "claude" 关键字的域名时，使用 SOCKS5 代理
  - DOMAIN-KEYWORD,claude,New-Proxy

  # 访问 Claude IDE 的请求都走代理，确保所有相关请求都经过代理
  - DOMAIN-KEYWORD,claude,New-Proxy

  # 其他流量直连
  - MATCH,Direct
